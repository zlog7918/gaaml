\begin{tikzpicture}[>=Latex]
  % nodes
  \node (in) at (0,0) {we};
  \node (h1) at (1,0) {u};
  \node (dots) at (2,0) {$\cdots$};
  \node (hN) at (3,0) {u};
  \node (out) at (4,0) {wy};
  \node[anchor=west] (where)
    at ($(out.east)+(0,-8pt)$)
    {, gdzie:}
  ;

  % arrows
  \draw[->] (in)--(h1);
  \draw[->] (h1)--(dots);
  \draw[->] (dots)--(hN);
  \draw[->] (hN)--(out);

  % brace
  \coordinate (a) at ($ (h1)+(-5pt,-5pt) $);
  \coordinate (b) at ($ (hN)+(5pt,-5pt) $);
  \draw[
    decorate,
    decoration={brace,mirror,amplitude=8pt},
  ] (a)--(b) node[midway,below=8pt] {$n$};
\end{tikzpicture}